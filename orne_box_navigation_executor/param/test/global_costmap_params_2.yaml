global_costmap:
   update_frequency: 0.2   #0.2
   map_topic: map # costmap用を使うか否か
   static_map: true  # 必須 static layerが機能しない
   transform_tolerance: 0.5
   static_layer:
   # map_topic: /map
    first_map_only: true        # ←これを追加！
  #  subscribe_to_updates: false # 地図の差分更新を受け取らない
