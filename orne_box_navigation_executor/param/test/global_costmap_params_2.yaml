global_costmap:
   #map_topic: map_for_costmap # costmap用を使うか否か
##   plugins:
 ##   - name: StaticLayer
  ##    type: costmap_2d::StaticLayer
  ##  - name: inflation_layer
  ##    type: costmap_2d::InflationLayer
  ##  StaticLayer: 
  ##   map_topic: map_for_costmap
#   static_map: true  # 必須 static layerが機能しない
   #robot_base_frame: base_link   # ロボットの基準となる座標フレーム。
   #update_frequency: 1.0         # コストマップの更新頻度（Hz単位、1秒間に1回更新）。
   #publish_frequency: 0.5        # 可視化用にコストマップを配信する頻度（Hz単位、1秒間に0.5回配信）。
   #transform_tolerance: 0.5      # TF座標変換の許容時間（秒単位、0.5秒以内）。 
   #global_frame: map       
#global_costmap:
 #  map_topic: map_for_costmap 
 #  static_map: true 
global_costmap:
   plugins:
    - name: StaticLayer
      type: costmap_2d::StaticLayer
    - name: inflationlayer
      type: costmap_2d::InflationLayer
   StaticLayer: 
     map_topic: map_for_costmap
